import 'package:flutter/material.dart';

class MatePage extends StatelessWidget {
  const MatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('MatePage'));
  }
}
